node_modules
yarn-error.log
dist
.vscode
.tmp
tmp
.eslintcache

# yarn v2
**/.yarn/cache
**/.yarn/unplugged
**/.yarn/build-state.yml
**/.yarn/install-state.gz
**/.pnp.*

.context

# Fixture install artifacts. The committed `_package.json` files are copied to
# `package.json` by `yarn setup-fixtures` so security scanners don't flag
# transitive deps inside fixtures.
fixtures/**/package.json
fixtures/**/yarn.lock
