on: [push]

jobs:
  compatibility-check:
    runs-on: ubuntu-latest
    name: Check compatibility with previous levitate versions
    steps:
      - name: Check compatibility
        id: levitate
        uses: grafana/levitate/actions/is-compatible@levitate-action-comment
        with:
          module: "./src/bin.ts"
          comment-pr: 'yes'
          fail-if-incompatible: 'yes'
