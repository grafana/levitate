import React from 'react';
/**
 * Horizontal section for editor components.
 *
 * @alpha
 */
export declare const SegmentSection: ({ label, children, fill, }: {
    label: string;
    children: React.ReactNode;
    fill?: boolean | undefined;
}) => JSX.Element;
