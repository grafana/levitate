import { VirtualElement } from '@popperjs/core/lib/types';
export declare class SelectionReference implements VirtualElement {
    getBoundingClientRect(): DOMRect;
    get clientWidth(): number;
    get clientHeight(): number;
}
