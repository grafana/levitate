import { OptionProps } from 'react-select';
export interface Props {
    children: Element;
}
export declare const NoOptionsMessage: (props: OptionProps<any, any>) => JSX.Element;
export default NoOptionsMessage;
